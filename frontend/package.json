{
  "name": "somnera-mattress-workspace",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend && node -e \"require('fs').cpSync('frontend/dist', 'dist', { recursive: true })\"",
    "test": "npm run test --prefix frontend"
  }
}
